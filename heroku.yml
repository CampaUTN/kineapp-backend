setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
release:
  command:
    - ./deployment-tasks.sh
run:
    web: gunicorn --pythonpath kinesio kinesio.wsgi --log-file -
